import { Typography, Container } from '@mui/material'

const Landing = () => {
   return   <Container>
                <Typography variant='h4'>Welcome to</Typography>
                <Typography variant='h1'>Clockwise</Typography>
            </Container>
}

export default Landing